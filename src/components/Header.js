import React from 'react';
import TopMenu from './TopMenu';

const Header = () => {
    return(
        <div className="header">
            <TopMenu />
        </div>
    );
}

export default Header;